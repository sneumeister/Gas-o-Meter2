# LP-Core Component (für zukünftige Implementierung)
# Aktuell leer - wird später implementiert

# idf_component_register(
#     SRCS "lp_core_main.c"
#     INCLUDE_DIRS "${CMAKE_CURRENT_SOURCE_DIR}"
# )
